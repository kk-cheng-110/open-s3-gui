name: Build and Release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    strategy:
      matrix:
        include:
          - os: macos-latest
            platform: mac
          - os: windows-latest
            platform: win
          - os: ubuntu-latest
            platform: linux

    runs-on: ${{ matrix.os }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Build icons
        run: npm run build:icons

      - name: Build renderer
        run: npm run build:renderer

      - name: Build Electron app (macOS)
        if: matrix.platform == 'mac'
        run: npm run build:mac -- --publish never

      - name: Build Electron app (Windows)
        if: matrix.platform == 'win'
        run: npm run build:win -- --publish never

      - name: Build Electron app (Linux)
        if: matrix.platform == 'linux'
        run: npm run build:linux -- --publish never

      - name: Upload artifacts (macOS)
        if: matrix.platform == 'mac'
        uses: actions/upload-artifact@v4
        with:
          name: macos-release
          path: |
            dist-app/*.dmg
            dist-app/*.zip
          retention-days: 5

      - name: Upload artifacts (Windows)
        if: matrix.platform == 'win'
        uses: actions/upload-artifact@v4
        with:
          name: windows-release
          path: |
            dist-app/*.exe
          retention-days: 5

      - name: Upload artifacts (Linux)
        if: matrix.platform == 'linux'
        uses: actions/upload-artifact@v4
        with:
          name: linux-release
          path: |
            dist-app/*.AppImage
            dist-app/*.deb
          retention-days: 5

  release:
    needs: build
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/')

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Display structure of downloaded files
        run: ls -R artifacts

      - name: Create Release
        uses: softprops/action-gh-release@v1
        with:
          files: |
            artifacts/macos-release/*
            artifacts/windows-release/*
            artifacts/linux-release/*
          draft: false
          prerelease: false
          generate_release_notes: true
          body: |
            ## macOS 安装说明
            
            如果遇到 **"应用已损坏"** 提示，请在终端执行：
            ```bash
            sudo xattr -d com.apple.quarantine /Applications/Open\ S3.app
            ```
            或者：
            ```bash
            xattr -c /Applications/Open\ S3.app
            ```
            
            也可以右键点击应用，按住 Option 键选择“打开”。
            
            详细说明请查看 [INSTALL-MACOS.md](https://github.com/kk-cheng-110/open-s3-gui/blob/main/INSTALL-MACOS.md)
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
